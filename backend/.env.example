# Copy to .env and fill. Never commit .env
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRE_MINUTES=480
# OCR / AI provider: stub | gemini
OCR_PROVIDER=stub
# Google Gemini (free tier key from Google AI Studio) — used when OCR_PROVIDER=gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash
