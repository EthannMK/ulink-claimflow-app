export interface Notif { id: string; icon: string; text: string; ref: string; at: string; unread: boolean }
// No demo data — notifications are added by the system or manually.
export const mockNotifs: Notif[] = []
